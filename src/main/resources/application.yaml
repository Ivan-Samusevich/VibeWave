server:
  port: 8080
  tomcat:
    connection-timeout: 60000

spring:
  application:
    name: VibeWave
  datasource:
    url: jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:VibeWave}
    username: ${PGUSER:postgres}
    password: ${PGPASSWORD:gfhjkm12}
  jpa:
    hibernate:
      show-sql: true
      generate-ddl: false
      jdbc:
        time_zone: Europe/Moscow
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

jwt:
  secret: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
  accessExpirationTime: 900000 #15 минут
  refreshExpirationTime: 604800000 #1 неделя
minio:
  url: ${MINIO_URL:http://localhost:9000}
  accessKey: ${MINIO_USER:admin}
  secretKey: ${MINIO_PASSWORD:password}
  bucket: ${MINIO_BUCKET:user-media}
